# CONTEXT.md — music-intel-mcp domain model

**Status: empty bootstrap.** This file is the authoritative source for terminology, invariants, and architectural decisions. It grows inline through `/grill` sessions — never as a one-shot doc.

The brainstorm in memory (`music_intel_mcp_project_revival`) is *input*, not the answer. Translating it to acceptance criteria, glossary entries, and invariants is the job of the next `/grill`.

---

## Glossary

_(Filled via `/grill`. Each term: one-line definition + cross-link to code/test where applicable.)_

## Invariants

_(Filled via `/grill`. Each invariant: assertion + where it's enforced — type, test, runtime check.)_

## Architectural decisions

_(Cross-references to memory `record_decision` episodes. Not duplicated here — memory is the queryable source of truth.)_

## Open questions

_(Filled via `/grill`. Each question: framing + decision tree branches + status.)_
